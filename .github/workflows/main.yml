name: interview

on:
    push:
        branches: ["main"]

jobs:
  build:
    name: Docker Build and Push to ECR
    runs-on: ubuntu-latest
    
    steps:
        - name: checkout
          uses: actions/checkout@v4

        - name: build image
          run: docker build -t testimg:1.0 .